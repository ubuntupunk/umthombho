{
  "name": "awomi-umthombho",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "prebuild": "node scripts/build-journal.js",
    "build": "echo 'No build required for static site'",
    "dev": "node scripts/build-journal.js && echo 'Static site - no dev server needed'"
  },
  "dependencies": {
    "@netlify/identity": "^1.0.0"
  },
  "devDependencies": {
    "gray-matter": "^4.0.3",
    "markdown-it": "^14.0.0",
    "glob": "^10.3.10"
  }
}
